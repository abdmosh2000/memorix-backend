module.exports = {
  // Line length
  printWidth: 100,
  
  // Tab width (spaces)
  tabWidth: 2,
  
  // Use spaces instead of tabs
  useTabs: false,
  
  // Always add semicolons
  semi: true,
  
  // Use single quotes
  singleQuote: true,
  
  // Object properties quoted only when necessary
  quoteProps: 'as-needed',
  
  // Use single quotes in JSX too
  jsxSingleQuote: false,
  
  // No trailing commas
  trailingComma: 'none',
  
  // Always use spaces inside brackets
  bracketSpacing: true,
  
  // Put the closing > of a JSX element on a new line
  jsxBracketSameLine: false,
  
  // Always use parentheses for arrow function arguments
  arrowParens: 'always',
  
  // Include space at beginning and end of file
  insertPragma: false,
  
  // Preserve prose formatting in markdown
  proseWrap: 'preserve',
  
  // HTML whitespace sensitivity
  htmlWhitespaceSensitivity: 'css',
  
  // End files with a newline
  endOfLine: 'auto'
};
